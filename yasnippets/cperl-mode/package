# -*- mode: snippet -*-
# name: package
# key: package
# --
package ${1:package};

use warnings FATAL => 'all';
use strict;
use utf8;
use Carp qw(croak confess);

use base qw(Exporter);
our @EXPORT_OK = qw();


1;
__END__
